import React from "react";

// Placeholder for future savepoint/version tagging UI
function SavepointTagger() {
  return (
    <div style={{ padding: 16, color: "#aaa", fontStyle: "italic" }}>
      Savepoint/version tagging UI coming soon.
    </div>
  );
}

export default SavepointTagger;

