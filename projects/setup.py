from setuptools import setup, find_packages

setup(
    name='ErrorLogger',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    description='A robust error logging utility for Python projects',
    author='Your Name',
    author_email='your.email@example.com',
    include_package_data=True,
)
